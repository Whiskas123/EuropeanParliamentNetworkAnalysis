# EuropeanParliamentNetworkAnalysis

To generate graphs for each of the countries, open *GenerateCountryGraphs.ipynb* and run

`G = generateGraph('NAME_OF_COUNTRY')`

you can also draw the graph by running

`drawGraph(G)`

